{
  "scripts": {
    "start": "live-server --host=localhost --port=3000",
    "learn": "live-server practice-jquery --open=jquery.html --host=localhost --port=3000",
    "watch": "npm run compile -- --watch",
    "compile": "tsc"
  },
  "devDependencies": {
    "live-server": "1.2.2",
    "typescript": "4.9.4"
  }
}