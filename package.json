{
  "scripts": {
    "start": "live-server --host=localhost --port=3000",
    "start:jquery": "live-server practice-jquery --open=jquery.html",
    "compile": "tsc",
    "watch": "npm run compile -- --watch"
  },
  "devDependencies": {
    "live-server": "1.2.2",
    "typescript": "4.9.4"
  }
}